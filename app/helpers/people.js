const people = () => {
  const dragonridersInSpace = 0;
  const dragonridersCumulative = 0;
  const moonPopulation = 0;
  const marsPopulation = 0;
  const employees = 7000;

  return {
    dragonridersInSpace,
    dragonridersCumulative,
    moonPopulation,
    marsPopulation,
    employees
  };
};

export default people;
