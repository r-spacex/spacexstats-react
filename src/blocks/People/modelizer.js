const modelizer = () => {
  const inSpace = 0;
  const moonPopulation = 0;
  const marsPopulation = 0;
  const employees = 7000;

  return {
    inSpace,
    moonPopulation,
    marsPopulation,
    employees,
  };
};

export default modelizer;
